import { useEffect, useState } from "react"; import axios from "axios"; export default function TransferHistory() { const [logs, setLogs] = useState([]); const token = localStorage.getItem( "token" ); useEffect(() => { fetchLogs(); }, []); const fetchLogs = async () => { try { const res = await axios.get( "http://localhost:5000/api/activity", { headers: { Authorization: `Bearer ${token}` } } ); const transferLogs = res.data.filter( (log) => log.action .toLowerCase() .includes( "transferred" ) ); setLogs( transferLogs ); } catch (error) { console.log(error); } }; return ( <div className="p-6 text-white"> <h1 className="text-3xl font-bold mb-6"> 🚚 Transfer History </h1> <div className="bg-slate-900 rounded-3xl overflow-hidden"> <table className="w-full"> <thead className="bg-slate-800"> <tr> <th className="p-4"> Action </th> <th> Role </th> <th> Date </th> </tr> </thead> <tbody> {logs.length === 0 ? ( <tr> <td colSpan="3" className="text-center p-8 text-slate-400" > No Transfers Yet </td> </tr> ) : ( logs.map( (log) => ( <tr key={ log._id } className="border-b border-slate-800 text-center" > <td className="p-4"> { log.action } </td> <td> { log.performedBy } </td> <td> { new Date( log.createdAt ).toLocaleString() } </td> </tr> ) ) )} </tbody> </table> </div> </div> ); }